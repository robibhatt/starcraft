# Project Guidelines

## Typing
- Be careful with typing to avoid VSCode/Pylance errors
- Use type assertions (`assert x is not None`) when narrowing types from BeautifulSoup
- Ensure function signatures match expected types
